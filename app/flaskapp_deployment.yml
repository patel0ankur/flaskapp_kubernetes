apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: flaskapp
  name: flaskapp

spec:
  replicas: 1
  selector:
    matchLabels:
      app: flaskapp
      tier: app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: flaskapp
        tier: app
    spec:
      containers:
        - name: flaskapp
          image: ankurpatel/kubeapp:v1
          env:
            - name: FLASK_ENV
              value: "development"
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secrets
                  key: ROOT_PASSWORD
            - name: MYSQL_HOST
              value: mysql
          ports:
            - name: flaskapp
              containerPort: 5000
          resources:
            requests:
              memory: 1Gi

      restartPolicy: Always
